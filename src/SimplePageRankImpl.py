
def page_rank(num_of_webpages, graph, num_iters=100):

    # initialize a dictionary with node value and a unit of importance
    pages = {node: 1 for node in graph.get_nodes()}

    for _ in range(num_iters):
        pass #TODO
